/reality-check:write-it-right Earlier in this session we went back and forth and
finally decided to drop the YAML config format in favor of TOML, because
YAML anchors kept confusing users — you talked me out of keeping both.
You also fixed the loader crash from last week. Now please write the
"Configuration" section of our README for the app "notekeeper": config
file location is ~/.notekeeper/config.toml, three keys are supported
(editor, autosave_seconds, theme), theme accepts "light" or "dark".
Write the section to a file named CONFIG-SECTION.md.
